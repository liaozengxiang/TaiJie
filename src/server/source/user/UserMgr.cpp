#include "UserMgr.h"

/*
 * 函数: Login
 * 功能: 用户请求登录，成功时将用户信息缓存在本地对象
 * 返回: 成功返回 True; 否则返回 False
 */
Bool CUserMgr::Login(LPCSTR lpszLoginID, LPCSTR lpszPassword)
{
    do
    {
    } while (0);

    return True;
}
